create table purchase(id serial primary key, date timestamp without time zone, store varchar(255));
create table items(id serial primary key, p_id serial references purchase(id), name varchar(255), price int, amount int, ean varchar(13), tax int);
